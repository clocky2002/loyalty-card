import tkinter # import tkinter

## Window declaration and windows settings
window = tkinter.Tk()
window.title("Check it out")
window.geometry("500x500")
#window.wm_iconbitmap('card.ico')

## Window elements (declare the elements: buttons, labels, textboxes etc)


## Window layout (layout the elements on the window [I think using .grid or .something-else])


## Initialise the window
window.mainloop()

